// import React from 'react';

// const Cart = ({ cart, onIncrement, onDecrement, onDelete }) => {
//   return (
//     <div>
//       {/* Cart UI and logic goes here */}
//       {cart.length === 0 ? (
//         <p>Your cart is empty</p>
//       ) : (
//         <ul>
//           {cart.map((item, index) => (
//             <li key={index}>
//               {item.title} - {item.quantity}
//               <button onClick={() => onIncrement(item)}>+</button>
//               <button onClick={() => onDecrement(item)}>-</button>
//               <button onClick={() => onDelete(item)}>Remove</button>
//             </li>
//           ))}
//         </ul>
//       )}
//     </div>
//   );
// };

// export default Cart;
